engine-strict=false
python=/usr/bin/python3
# Hostinger's build container lacks python/make; better-sqlite3 has prebuilds so no rebuild needed.
# If install still tries node-gyp, set NPM_CONFIG_IGNORE_SCRIPTS=true in Hostinger Environment Variables
# and run patches manually via Build command: node scripts/postinstall.js && npm run buildall
